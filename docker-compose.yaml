version: "3.8"
services:
  bind-dns:
    build: 
      context: bind-dns/
    networks: 
      - internal

  bastion:
    build:
      context: bastion/
    tty: true
    networks: 
      - internal
      - bridge
    

networks:
  internal:
  bridge: